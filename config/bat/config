# Can run `bat --help` to get a list of all possible configuration options.

# Run `bat --list-themes` for a list of all available themes
--theme="TwoDark"

# Enable this to use italic text on the terminal.
--italic-text=always

# Uncomment the following line to disable automatic paging:
--paging=never

